# Ennegram v0.1 Review

## Context

- Review date: 2026-04-18
- Repository: CampaignGenerator
- Evaluation window: Day 3-Day 10 (Ennegram v0.1 checklist execution)
- Reviewer: kostadis

## Baseline vs Pilot Metrics

| Metric | Baseline | Pilot | Delta | Notes |
|---|---:|---:|---:|---|
| Time-to-first-acceptable output | n/a (not measured pre-pilot) | ~2 min | qualitative improvement | Day 8-Day 9 recalls consistently returned actionable wiki-first context quickly. |
| Context misses per task | n/a (not measured pre-pilot) | 0.25 | improving within pilot | Day 9 tightening reduced generic fallback misses via line-anchored fallback scoring. |
| Recall provenance coverage (%) | n/a (not measured pre-pilot) | 100 | strong pilot baseline established | Provenance + confidence present on all reviewed recall rows. |
| Stale canonical pages (%) | n/a (not measured pre-pilot) | 0 | healthy current state | Validation clean on current set (`errors=0`, `warnings=0`, `files=4`). |
| Trust score (1-5) | n/a (not measured pre-pilot) | 4.0 | good, with room to improve | High trust in wiki-first results; fallback noise still requires manual triage. |

## What Worked

- Day 3 init confidence check passed: `make en-init` re-runs are idempotent.
- Day 5 recall checks returned provenance + confidence on all top results across two live queries.
- Day 8 pilot: active tasks (scene editor chunk pattern, config precedence) both surfaced `architecture` as top wiki context with usable confidence.
- Day 9 tightening improved fallback provenance specificity from generic `#top` to line anchors (e.g. `server/config.py#L107`) for implementation-detail queries.
- 

## What Did Not Work

- Minor UX friction: `init` always prints "Initialized Ennegram directories." even when no changes are made, which can imply work happened on no-op reruns.
- Recall granularity is still coarse: results often return page/file `#top` summaries, and fallback rankings can include semantically weak low-confidence matches.
- Day 8 pilot: fallback ordering still pulled unrelated generic entries (`planning.py#top`, `session_doc.py#top`) for config/splitter questions.
- Day 9 still shows occasional semantically off low-confidence fallback lines, but with clearer anchors and easier manual triage.
- 

## Day 3 Setup Notes

- Commands run: `make en-init` twice in succession.
- Idempotency check method: SHA-256 hashes across all files under `CampaignGenerator/ennegram` captured before and after reruns.
- Result: `changed=0`, `added=0`, `removed=0` (no unexpected file overwrites).

## Day 5 Recall Notes

- Query 1: `make en-recall QUERY="where is split strategy configured for extractors"`.
- Query 2: `make en-recall QUERY="how do ui_defaults and ui_config precedence work"`.
- Provenance/confidence status: present on every result row (wiki + fallback).
- Observed edge case: low-confidence fallback can surface generic top-of-file hits (`planning.py#top`, `session_doc.py#top`) even when higher-signal targets exist.

## Day 6 Validation Snapshot

- Command: `make en-validate` (re-run twice to confirm stability).
- Report file: `CampaignGenerator/ennegram/reports/validate_report.json`.
- Snapshot: `files_scanned=4`, `errors=0`, `warnings=0`.
- Snapshot timestamp: `2026-04-18T03:35:19.911343+00:00`.

## Day 8 Pilot Session A

- Task 1: Scene editor chunk-field pattern validation (`ChunkRef`-compatible behavior).
- Task 2: `ui_defaults` vs `ui_config` precedence verification for split defaults.
- Recall commands run:
  - `make en-recall QUERY="for scene_editor chunk fields, what is the ChunkRef-compatible pattern"`
  - `make en-recall QUERY="what precedence should ui_defaults and ui_config use for split defaults"`
- What helped:
  - Fast wiki-first anchoring to `architecture.md` for both active tasks.
  - Confidence/provenance visibility made result trust decisions explicit.
- What slowed down:
  - Fallback lists still need manual filtering due to generic top-of-file matches.

## Day 9 Pilot Session B + Tightening

- Task 1: `make en-recall QUERY="where is scene_editor chunk range resolved before API response"`.
- Task 2: `make en-recall QUERY="how are global_extractor_split_chapters defaults applied"`.
- Targeted friction fixes (top 2 only):
  1. Added line-level fallback anchoring in `ennegram_cli.py` so fallback provenance points to best matching lines instead of `#top`.
  2. Added specificity-weighted fallback scoring (line match + file match blend) to reduce generic top-of-file ranking bias.
- Post-fix behavior:
  - Fallback provenance now includes line anchors (e.g. `server/config.py#L107`).
  - Split-default query correctly surfaces `server/config.py` as top code fallback.
- Reliability loop:
  - Re-ran `make en-ingest && make en-validate` after fixes; validation remained clean.

## Residual Risks

- Fallback ranking still occasionally returns semantically off low-confidence lines, even with line-level anchoring.
- Knowledge base breadth is currently only 4 indexed wiki files, so uncovered domains still rely on weaker fallback search.
- Baseline metrics were not captured before Day 3, limiting strict KPI delta claims for v0.1.

## Corrections Summary

- Total corrections logged: 4
- Most corrected entries: `caveats` (2), `runbook` (1), `planning.py` (1)
- Recurring correction themes: low-confidence fallback rankings returning generic `#top` file hits for implementation-detail queries
- Actions taken: added "Recall precision guardrail" to `ennegram/wiki/runbook.md`; re-indexed and re-validated (`make en-ingest && make en-validate`)

## Guardrail Status

- [x] Canonical truth model preserved
- [x] Provenance present on recalls used in decisions
- [x] Validation run regularly
- [x] Drift handled within expected cadence

## v0.2 Candidates (Max 3)

1. Add section-level indexing/anchors for wiki pages so recall returns scoped sections instead of frequent `#top` summaries.
2. Introduce fallback re-ranking with symbol/file intent boosts (e.g. prioritize `server/config.py` for config-precedence queries).
3. Add pre-pilot metric bootstrap command/report so baseline deltas are measured quantitatively from day 1.

## Decision

- [ ] Continue
- [x] Continue with narrowed scope
- [ ] Pause and reassess

Rationale:

- v0.1 core loop (`init -> ingest -> recall -> validate -> correct`) is functional and stable in pilot use.
- Reliability is acceptable (idempotent init, clean repeated validation/ingest, correction loop active).
- Remaining gaps are quality/precision tuning and measurement rigor, best addressed via focused v0.2 improvements rather than broad scope expansion.
